Write a dense, keyword-rich summary of the project here. This is the ONLY text fed into the
semantic search embedding — nothing else. Aim for 200–400 words.

Include:
- What the project IS (medium, format, genre)
- What it's ABOUT (themes, subject matter, concepts)
- How it was MADE (tools, methods, techniques)
- Where/when (institution, year, context)
- Specific vocabulary a visitor might search for

Example queries this text should match:
- "master thesis", "photogrammetry", "3D printing", "posthuman", "AI", "storytelling"

Do NOT use markdown formatting — plain prose only.
